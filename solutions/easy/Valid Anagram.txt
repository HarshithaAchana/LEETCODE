// Title: Valid Anagram
            // Difficulty: Easy
            // Language: Python3
            // Link: https://leetcode.com/problems/valid-anagram/

        if len(a_Array) != len(b_Array) :
            return False
        for i in a_Array :
            if   i in b_Array:
                b_Array.remove(i)
        if len(b_Array) == 0 : 
            return True
        else :
            return False
        b_Array=[i for i in t] 
        a_Array=[i for i in s]
    def isAnagram(self, s: str, t: str) -> bool:
class Solution:
